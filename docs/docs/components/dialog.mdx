---
sidebar_position: 9
title: Dialog
description: Lightweight semantic dialog component for ignix-lite with intent variants and accessible modal interactions.
---

import Tabs from '@theme/Tabs'
import TabItem from '@theme/TabItem'
import Preview from '@site/src/components/preview'

# Dialog

The Dialog component provides a lightweight and semantic way to display modal content using the native HTML `<dialog>` element.

Ignix Lite dialogs support intent variants, accessible focus management, and clean modal interactions without requiring JavaScript frameworks or external dependencies.

---

## Preview

<Tabs defaultValue="preview">

<TabItem value="preview" label="Preview">

<Preview>

<div style={{ display: 'flex', gap: '1rem', flexWrap: 'wrap' }}>

<button
  data-intent="danger"
  onClick={() => {
    const dialog = document.querySelector('#danger-dialog')

    if (dialog instanceof HTMLDialogElement) {
      dialog.showModal()
    }
  }}
>
  Delete Item
</button>

<button
  data-intent="warning"
  onClick={() => {
    const dialog = document.querySelector('#warning-dialog')

    if (dialog instanceof HTMLDialogElement) {
      dialog.showModal()
    }
  }}
>
  Open Warning
</button>

<button
  onClick={() => {
    const dialog = document.querySelector('#info-dialog')

    if (dialog instanceof HTMLDialogElement) {
      dialog.showModal()
    }
  }}
>
  Open Info
</button>

</div>

<dialog id="danger-dialog" data-intent="danger">

  <h2>Confirm Delete</h2>

  <p>
    Are you sure you want to delete this item?
  </p>

  <p>
    This action cannot be undone.
  </p>

  <div
    style={{
      display: 'flex',
      gap: '0.75rem',
      marginTop: '1rem',
    }}
  >

    <button
      style={{
        all: 'unset',
        padding: '0.6rem 1rem',
        border: '1px solid var(--ix-border)',
        borderRadius: 'var(--ix-radius)',
        cursor: 'pointer',
        background: 'var(--ix-surface)',
        color: 'var(--ix-text)',
      }}
      onClick={() => {
        const dialog = document.querySelector('#danger-dialog')

        if (dialog instanceof HTMLDialogElement) {
          dialog.close()
        }
      }}
    >
      Cancel
    </button>

    <button
      style={{
        all: 'unset',
        padding: '0.6rem 1rem',
        borderRadius: 'var(--ix-radius)',
        cursor: 'pointer',
        background: 'var(--ix-danger)',
        color: 'var(--ix-on-danger)',
      }}
      onClick={() => {
        const dialog = document.querySelector('#danger-dialog')

        if (dialog instanceof HTMLDialogElement) {
          dialog.close()
        }
      }}
    >
      Delete
    </button>

  </div>

</dialog>

<dialog id="warning-dialog" data-intent="warning">

  <h2>Unsaved Changes</h2>

  <p>
    You have unsaved changes.
  </p>

  <p>
    Leaving now may result in data loss.
  </p>

  <div>

    <button
      onClick={() => {
        const dialog = document.querySelector('#warning-dialog')

        if (dialog instanceof HTMLDialogElement) {
          dialog.close()
        }
      }}
    >
      Stay
    </button>

    <button
      
      onClick={() => {
        const dialog = document.querySelector('#warning-dialog')

        if (dialog instanceof HTMLDialogElement) {
          dialog.close()
        }
      }}
    >
      Continue
    </button>

  </div>

</dialog>

<dialog id="info-dialog" data-intent="info">

  <h2>Session Expiring</h2>

  <p>
    Your session will expire in 2 minutes.
  </p>

  <p>
    Save your work before continuing.
  </p>

  <div>

    <button
      onClick={() => {
        const dialog = document.querySelector('#info-dialog')

        if (dialog instanceof HTMLDialogElement) {
          dialog.close()
        }
      }}
    >
      Close
    </button>

    <button
      onClick={() => {
        const dialog = document.querySelector('#info-dialog')

        if (dialog instanceof HTMLDialogElement) {
          dialog.close()
        }
      }}
    >
      Save Work
    </button>

  </div>

</dialog>

</Preview>

</TabItem>

<TabItem value="code" label="Code">

```html
<button onclick="confirmDialog.showModal()">
  Delete Item
</button>

<dialog id="confirmDialog" data-intent="danger">

  <h2>Confirm Delete</h2>

  <p>
    Are you sure you want to delete this item?
  </p>

  <p>
    This action cannot be undone.
  </p>

  <button onclick="confirmDialog.close()">
    Cancel
  </button>

  <button
    data-intent="danger"
    onclick="confirmDialog.close()"
  >
    Delete
  </button>

</dialog>
```

</TabItem>

</Tabs>

---

## Installation

<Tabs defaultValue="npm">

<TabItem value="npm" label="npm">

```bash
npm install @mindfiredigital/ignix-lite
```

</TabItem>

<TabItem value="cdn" label="CDN">

```html
<link
  rel="stylesheet"
  href="https://unpkg.com/@mindfiredigital/ignix-lite/dist/ignix-lite.min.css"
/>
```

</TabItem>

</Tabs>

---

## Usage

```html
<dialog id="dialog">

  <h2>Dialog Title</h2>

  <p>
    Dialog content goes here.
  </p>

  <button onclick="dialog.close()">
    Close
  </button>

</dialog>

<button onclick="dialog.showModal()">
  Open Dialog
</button>
```

---

## Intent Variants

```html
<dialog data-intent="danger"></dialog>

<dialog data-intent="warning"></dialog>

<dialog data-intent="info"></dialog>
```

---

## Attributes

| Attribute | Type | Description |
|---|---|---|
| `data-intent` | `'danger' \| 'warning' \| 'info'` | Applies semantic dialog styling |
| `open` | `boolean` | Native dialog open state |
| `id` | `string` | Unique dialog identifier |
| `showModal()` | `method` | Opens the dialog as a modal |
| `close()` | `method` | Closes the dialog |