var t = class extends HTMLElement {
  constructor() {
    super();
  }
  connectedCallback() {
    this.hasAttribute("tabindex") || (this.tabIndex = 0), this.hasAttribute("role") || this.setAttribute("role", "tooltip");
  }
};
customElements.get("ix-tooltip") || customElements.define("ix-tooltip", t);
